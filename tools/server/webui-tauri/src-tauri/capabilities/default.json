{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Default permissions plus the webview-window creation surface the pop-out buttons need (terminal drawer in chat → term-*; artifact detail → art-*). The globs cover every spawned window without needing per-id capability entries.",
	"windows": [
		"main",
		"term-*",
		"art-*"
	],
	"permissions": [
		"core:default",
		"core:webview:allow-create-webview-window",
		"core:webview:allow-webview-close",
		"core:window:allow-set-focus",
		"core:window:allow-set-title",
		{
			"identifier": "http:default",
			"allow": [
				{ "url": "http://127.0.0.1:*/*" },
				{ "url": "http://localhost:*/*" }
			]
		}
	]
}
